<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="usermapper">
    <select id="selectByUsername" resultType="entity.User">
        select * from sys_user where username = #{value}
    </select>

    <insert id="insertUser" parameterType="entity.User" useGeneratedKeys="true" keyColumn="user_id" keyProperty="userId">
        insert into sys_user (username, password, employee_id, salt) values (#{userName}, #{password}, #{employeeId}, #{salt})
    </insert>
</mapper>